# CHANGELOG — TELA CLUB Random Match Generator

---

## v5.6 — 2026-05-30

### 🔴 버그수정

**`_get_gsheet_connection is not defined` 오류**
- 원인: `_restore_shelf_from_gsheet()` 호출이 `set_page_config` 직후(1946줄)에 있었는데,
  내부에서 사용하는 `_get_gsheet_connection`은 2107줄에 정의됨
  → 모듈을 위→아래로 실행하는 Python 특성상 함수 정의 전 호출로 `NameError` 발생
- 수정: `_restore_shelf_from_gsheet()` 호출을 `_get_gsheet_connection` 정의 직후로 이동

### 🟢 신규

**스코어보드 잠금 기능**
- 확정된 대진표·점수를 잠금하여 수정 불가 상태로 전환
- 잠금 상태는 `schedules` 탭 `is_locked` 컬럼에 저장 (앱 재시작 후에도 유지)

| 동작 | 조건 |
|------|------|
| 잠금 | 관리자 로그인 → `🔒 스코어보드 잠금 관리` expander → `🔒 잠금` 버튼 1클릭 |
| 잠금 해제 | 관리자 비밀번호 재입력 필수 → `🔓 잠금 해제` 버튼 |
| 잠금 중 표시 | 상단 빨간 배너 "🔒 이 스코어보드는 잠금 상태입니다." |
| 잠금 중 제한 | 점수 입력·수정·초기화 버튼 전체 숨김 (`_can_edit = False`) |
| 비관리자 | 잠금 관련 UI 아예 미표시 |

---

## v5.5 — 2026-05-29

### 🔴 버그수정

**페어 수동 조정 적용 메시지 불규칙 표시**
- 원인: `st.success()` 직후 `st.rerun()`으로 메시지가 렌더링 전에 사라짐
- 수정: `session_state["_adj_success_msg"]`에 저장 → rerun 후 표시 → 자동 삭제

**휴면 시작 전 회원이 참가자 선택에서 차단되는 문제**
- 원인1: `rp_date_inp` (존재하지 않는 키) → 오늘 날짜 fallback → 경기일 기준 판단 불가
- 원인2: `category == "휴면"` 이면 날짜 무관하게 참가 차단
- 수정: 실제 위젯 key `"rp_date"` 로 수정, `parse_dormant_periods()` 로 기간 파싱하여 경기일 기준 판단

| 상황 | 처리 |
|------|------|
| 6/1 휴면, 경기일 5/23 | ✅ 참가 가능 (시작 전) |
| 5/1 휴면, 경기일 5/23 | ❌ 참가 불가 (기간 중) |
| 무기한 휴면, 시작일 이후 | ❌ 참가 불가 |
| dormant_period 없고 category=휴면 | ❌ 참가 불가 (레거시 호환) |

**회원 정보 수정 시 league 초기화 버그**
- 원인: `dialog_form`의 `row_data`에 `"league"` 키 누락 → 수정 저장 시 항상 `""` 덮어씀
- 수정: `"league": existing.get("league", "")` 추가 — 기존 값 보존

**미래 휴면 등록 시 category 즉시 "휴면"으로 설정**
- 원인: `has_ongoing` 판단 시 시작일이 미래여도 종료일 없으면 `True`
- 수정: 시작일 ≤ 오늘인 경우만 `has_ongoing = True` 처리

**참가자 인원 카운트 오류**
- 원인: `selected_members` dict(누적 오염) 기준으로 카운트 → 실제 체크와 불일치
- 수정: 실제 위젯 키 `mchk_{lg}_{id}` / `gchk_{lg}_{name}` 직접 조회로 교체

**전체선택·전체해제 시 팝업 닫힘**
- 원인: `@st.dialog` 내부에서 `st.rerun()` 호출 → 팝업 닫힘
- 수정: `st.rerun()` 제거, `session_state` 위젯 키 직접 설정으로 즉시 반영

---

## v5.4 — 2026-05-29

### 🔴 버그수정

**Streamlit Cloud 재시작 시 모든 로컬 데이터 소실**

- 원인: Streamlit Cloud는 앱이 비활성 상태에서 컨테이너를 재시작하며 로컬 파일시스템 초기화
- 결과: `.tela_data/` 폴더 전체 삭제 → 대진표·점수·계정·게스트·제외 선수 모두 소실
- 구글시트 연동 데이터(records, 회원명부)는 영향 없음


### 🟢 신규

**전체 shelve 데이터 구글시트 이중화 (재시작 후 자동 복원)**

저장 시 로컬 shelve + 구글시트에 동시 저장.
앱 시작 시 `_restore_shelf_from_gsheet()` 자동 실행 → 구글시트에서 로컬 복원 (세션당 1회).

**`schedules` 탭 — 대진표·점수 영구 저장**

|함수                  |동작                                       |
|--------------------|-----------------------------------------|
|`shelf_save()`      |로컬 shelve 저장 후 `_gsheet_sched_save()` 동기화|
|`shelf_load()`      |로컬 우선 조회 → 없으면 `_gsheet_sched_load()` 복원 |
|`shelf_list_dates()`|로컬 우선 → 없으면 `_gsheet_sched_list()` 반환    |
|`shelf_delete()`    |로컬 + `_gsheet_sched_delete()` 동시 삭제      |

저장 컬럼:
`date_key` · `is_fully_random` · `match_idx` · `round` · `league` ·
`team1` · `team2` · `type` · `exclude_players` · `score1` · `score2` · `is_dup`

**`settings` 탭 — key-value JSON 이중화**

|key      |내용           |함수                   |
|---------|-------------|---------------------|
|`users`  |계정·비밀번호 해시·권한|`user_save_all()`    |
|`guests` |게스트 목록       |`guest_save()`       |
|`exclude`|기록실 제외 선수    |`exclude_list_save()`|

헬퍼: `_settings_gsheet_get(key)` · `_settings_gsheet_set(key, value)`
복원: `_settings_restore_all()` — 앱 시작 시 각 shelve에 자동 적재

> **회원 데이터(members)는 이중화 미적용.**
> 회원 데이터는 구글시트 `sheet1`이 원본이므로 shelve 복사본이 불필요.


### 🛠 코드 정리 (죽은 코드 제거 — 5778줄 → 5570줄, **-208줄**)

|제거 항목                                           |줄 수|제거 사유                             |
|------------------------------------------------|---|----------------------------------|
|`sync_from_sheet`                               |91 |회원 연동이 `load_df` 직접 방식으로 대체됨      |
|`save_league_by_name`                           |23 |`save_league_to_sheet`(id 기준)로 대체 |
|`get_sheet`                                     |20 |`load_df`가 컬럼 자동 보정 처리, 마이그레이션 불필요|
|`_get_gspread_client`                           |14 |`_get_gsheet_connection`으로 통일됨    |
|`member_save_all` / `member_load_all`           |13 |회원은 구글시트 sheet1에서 직접 관리           |
|`member_add`                                    |8  |동일                                |
|`member_remove`                                 |5  |동일                                |
|`_is_excluded_player`                           |4  |`_excluded_set` 직접 조회로 대체됨        |
|`has_ongoing_dormant`                           |2  |미사용                               |
|`MEMBER_PATH` · `SHEET_ID` · `UNASSIGNED_KEY` 상수|3  |미사용                               |

**점검 결과 이상 없음:**

- 모듈 레벨 중복 함수 없음
- `bare except:` 0개 (전부 구체적 예외 처리)
- `print()` 디버그 잔재 없음
- TODO / FIXME 없음
- 중복 import 없음

---

## v5.3 — 2026-05-26

### 🔴 버그수정

**기록실 누적 데이터 정합성 오류 (핵심 수정)**
- 원인1: `records_commit`이 집계 결과가 빈 경우(점수 미입력·전원 제외) 기존 행 삭제 전에 조기 return → 옛 데이터 영구 잔존
- 원인2: 대진표 삭제 시 `shelf_delete()`만 호출하고 구글시트 records 탭은 삭제하지 않음
- 원인3: 페어 수동 조정 시 점수가 없으면 `records_commit` 미호출 → 제외 설정이 기록실에 미반영
- 수정: `records_commit` 재구조화 — ① 기존 행 삭제(항상 실행) → ② 새 통계 계산 → ③ 새 행 삽입
- 수정: `records_delete_by_date(date_key)` 헬퍼 신규 추가
- 수정: 대진표 삭제 시 `records_delete_by_date()` + `cache_data.clear()` 자동 호출
- 수정: 페어 수정 시 점수 유무와 무관하게 항상 `records_commit` 호출

**페어 수동 조정 기록 제외 미반영**
- 원인: 체크박스는 존재했으나 `exclude_players` 필드만 저장되고 코드에 `(중복)` 태그가 붙지 않음
  → `display_name()`이 코드 내 `(중복)` 문자열을 기준으로 작동하여 화면·집계 모두 미반영
- 수정: 페어 적용 시 `exclude_players`에 포함된 선수 코드에 `(중복)` 접미사 자동 추가
- 수정: 체크 해제 시 기존 `(중복)` 태그 자동 제거
- 수정: 매치 type에도 `(중복)` 표기 (경기카드 우측 하단 표시)

**복원 대진표 탭 페어 수동 조정 체크박스 미표시**
- 원인: 이전 수정이 신규 생성 탭(`adj1`)에만 적용되고 복원 탭(`adj2`)은 구버전 코드 그대로
- 수정: `adj2` 탭도 신규 생성 탭과 동일 구조로 전면 교체 (selectbox 바로 아래 개인별 체크박스 배치)

**모바일 스코어보드 레이아웃 깨짐**
- 원인: `st.columns` 가 모바일에서 자동 세로 스택 전환, `min-width:0` CSS 1개로는 부족
- 수정: CSS 전면 강화 (`flex-wrap:nowrap`, `flex-shrink:1`, number_input 최소 너비 제거, 버튼 패딩 축소)
- 수정: 저장 완료 행 4컬럼 → 3컬럼 `[4,3,4]` (점수·수정버튼·점수)
- 수정: 입력 행 `[3,2,2,3]` → `[4,2,2,4]` (점수 입력칸 확대)
- 수정: 버튼 텍스트 `"💾 저장"` → `"💾"`, `"✖ 취소"` → `"✖"` (이모지만, help 툴팁 추가)
- 수정: 경기카드 팀명 `text-overflow:ellipsis` + `white-space:nowrap` 적용

**로그아웃 후 다른 페이지 선택 시 기록실로 초기화**
- 원인: `st.sidebar.radio(index=0)` 하드코딩 → `rerun()` 시 항상 첫 번째 메뉴로 리셋
- 수정: `st.session_state["current_page"]`로 현재 페이지 저장 → 로그아웃/rerun 후에도 마지막 선택 유지

**기록실 득점 셀 배경색 잔존**
- 원인: Streamlit `st.dataframe`의 숫자 컬럼 자동 그라데이션 배경
- 수정: `column_config.NumberColumn(format="%d")`를 수치 컬럼 전체에 명시 적용

**스코어보드 초기화 버튼 비회원 노출**
- 수정: `is_admin()` 조건 추가 → 관리자만 표시

### 🟢 신규

**중복 기록 제외 — 개인별 설정 (위치 이동)**
- 기존: 스코어보드 입력행 경기 단위 체크박스
- 변경: 대진표생성 > 관리자 페어 수동 조정 > 각 selectbox 바로 아래 개인별 `🚫 기록 제외` 체크박스
- 저장: schedule dict의 `exclude_players: [선수코드, ...]` 필드에 영구 저장
- 표시: 체크된 선수 코드에 `(중복)` 태그 자동 추가 → 대진표·스코어보드에 이름 옆 `(중복)` 표시
- 집계: `_records_build_session_stats`에서 `exclude_players` + `(중복)` 태그 모두 감지 후 제외

**페어 적용 확인 메시지 버튼 옆 표시**
- 버튼 컬럼 `[2, 6]`으로 분할 → 오른쪽 컬럼에 `st.success` 인라인 표시
- 제외 선수 있을 경우: `✅ #N 적용 완료 (제외: 이름1, 이름2)` 표기

**기록실 출전경기 컬럼 추가**
- `wins + losses`로 출전경기 수 계산 → 순위·이름 다음 컬럼에 표시
- 컬럼 순서: 순위 · 이름 · 출전경기 · 승 · 패 · 득점 · 실점 · 득실차 · 승률

**기록실 연간 통합 수상 카드**
- 연간 모드 선택 시 전 리그 통합 득점왕·다승왕·승률왕 카드 상단 표시
- 카드 제목: `26년 통합 득점왕` 형식 (연도 2자리 표기)

**계정 ID 영문+숫자만 허용**
- 신규 계정 추가 시 `re.match(r'^[A-Za-z0-9]+$')` 검증
- 한글·특수문자 포함 시 에러 메시지 표시
- 로그인 입력창 placeholder `"영문+숫자 ID"` 안내 문구 변경

### 🔵 변경

**명칭·표기 변경**
- 기록실 월간 카드: `A리그 2026-05 최다득점` → `A리그 26년 05월 최다득점`
- 기록실 연간 카드: `2026 득점왕` → `26년 통합 득점왕`

**사이드바 메뉴 순서 변경**
- 기록실 → 스코어보드 → 대진표생성 → 회원명부

**스코어보드 모드 기본값 변경**
- `새 스코어보드` 우선 → `저장된 스코어보드 불러오기` 우선으로 변경

**기록실 순위·정렬 기준 변경**
- 기존: 승수 기준 → 변경: 득점↓ → 득실차↓ → 승↓ 순 정렬
- 좌측에 `순위` 컬럼 추가 (리그별 독립 순위)

**수상 카드 순서 변경**
- 기존: 다승왕·득점왕·승률왕 → 변경: 득점왕·다승왕·승률왕

**+/- 버튼 배경색 개선**
- 연두색 배경(`#e8f5e9`) + 초록 테두리(`#a5d6a7`) 적용
- 숫자 입력칸 연한 노랑-초록(`#f9fbe7`) 배경

**`serialize` / `deserialize_schedule` 확장**
- `exclude_players` 필드 포함하여 저장·복원 (기존 데이터 하위 호환)

**회원명부 비로그인 안내 텍스트**
- "운영진이 아닌 일반 회원은 제한된 열람만 가능합니다." 문구 추가

**페어 수동 조정 selectbox key 수정**
- 고정 문자열 key → `f"adj_t1a_{_sel_mi}"` 형태로 변경
- 경기 선택 변경 시 위젯이 새로 생성되어 이전 선수 잔존 버그 해결

---

## v5.20 — 2026-05-26

### 🔵 변경

**기록실 카드 문구 자연어화**
- 월간 카드: `A리그 2026-05 최다득점` → `A리그 26년 05월 최다득점`
- 연간 카드: `2026 득점왕` → `26년 통합 득점왕` (전 리그 통합 표기)

**스코어보드 관리자 전용 입력**
- 저장·수정 버튼: 관리자만 노출 (일반 회원은 점수 확인만 가능)
- 취소 버튼 추가: 수정 중 이전 저장값으로 복귀

**모바일 점수 입력 한 줄 처리**
- `[점수1][저장][취소][점수2]` 4컬럼 고정 레이아웃 적용

**기록실 순위 및 정렬 기준**
- 득점↓ → 득실차↓ → 승↓ 순 정렬
- 테이블 좌측에 `순위` 컬럼 추가

**사이드바 메뉴 순서 변경**
- 기록실 → 스코어보드 → 대진표생성 → 회원명부

**중복 경기 수동 처리**
- 저장 시 "중복 경기로 처리" 체크박스 추가 → 기록실 집계 자동 제외

**회원명부 안내 텍스트 수정**
- "운영진이 아닌 일반 회원은 제한된 열람만 가능합니다." 문구 추가


---

## v5.10 — 2026-05-26

### 🔴 버그수정

**스코어 저장 완전 해결**
- 원인: `window.top.location.href` 방식이 Streamlit Cloud iframe 보안 정책(`X-Frame-Options`)과 충돌 → URL 변경 차단으로 저장 실패
- 해결: HTML 컴포넌트 점수 입력 UI 제거 → Streamlit native `number_input` + `button` 방식으로 전면 교체
- 💾 저장 클릭 → `shelf_save()` 즉시 실행 → 화면 즉시 갱신
- 구글시트 기록(`records_commit`)은 백그라운드 스레드 처리 → 딜레이 제거
- 새로고침 후 shelf에서 점수·locked 상태 정확히 복원

**선수별 현황 작동 안 하는 문제**
- `sb_scores`가 빈 dict일 때 `shelf_load()` fallback 추가
- `compute_scoreboard_stats` 전면 재작성
  - `(중복)` 태그 선수: 출전·승·패·득점·실점 모두 제외
  - 제외 목록 선수(코치 등): 완전 제외
  - 게스트(`★` 코드): 자동 감지 후 완전 제외 (수동 등록 불필요)
  - 컬럼명 `승점` → `득점` 통일

### 🟢 신규

**기록실 (누적 랭킹 통계)**
- 사이드바 독립 메뉴로 추가
- 구글시트 `records` 탭에 자동 저장
  - 컬럼: `date_key`, `year_month`, `year`, `player_key`, `display_name`, `league`, `wins`, `losses`, `pf`, `pa`
  - 동일 `date_key` 재저장 시 기존 행 삭제 후 재삽입 (중복 방지)
  - TTL 120초 캐시 (`@st.cache_data`) — API 호출 최소화
- 월간 / 연간 탭 구분 조회
- 리그별 수상 카드: 🥇 다승왕 · 🎯 득점왕 · 👑 승률왕
- 관리자 제외 선수 설정 UI (코치 등 영구 제외, shelve 저장)
- 전체 날짜 일괄 재집계 버튼 (제외 선수 추가 후 소급 적용)

**player_key 접두사 정제**
- `AM윤지수` → `윤지수`, `AW최선화` → `최선화`
- 리그 변경 시에도 동일 선수로 누적 집계

**관리자 수동 페어 조정**
- 대진표 탭 내 `🔧 관리자: 페어 수동 조정` expander
- 경기 선택 → 리그 내 선수 재배정 → `rp_schedule` · `sb_schedule` · shelf 즉시 업데이트
- 신규 생성 대진표 + 복원 대진표 양쪽 모두 적용

### 🔵 변경

**명칭 통일**

| 기존 | 변경 |
|------|------|
| 📊 점수판 | 📊 스코어보드 |
| 🎲 랜덤페어 | 📋 대진표생성 |
| TELA 테니스 클럽 랜덤페어 점수판 | TELA 클럽 랭킹리그 스코어보드 |
| 브라우저 탭 `TELA CLUB v5.00` | `TELA CLUB v5.10` |
| 사이드바 | `TELA TENNIS CLUB` + `v5.10` 캡션 추가 |
| 회원명부·대진표생성 본문 버전 표기 | 제거 (사이드바 단일 표기) |

### ⚡ 성능개선

- `exclude_list_load()` 반복 호출 제거: 경기 수만큼 shelve 반복 read → 함수 진입 시 1회만 로드 후 set 캐싱

---

## v5.00 — 2026-05-23

### 🟢 신규

**앱 로그인 / 권한 시스템**
- 사이드바 아이디·비밀번호 로그인 (엔터키 지원)
- 쿠키 기반 로그인 유지 (`extra_streamlit_components`, 30일)
- URL `query_params` 토큰 방식 병행 (새로고침 로그인 유지)
- 권한 분리: 관리자 / 회원 / 비로그인
- 관리자 전용 계정 관리 expander (추가·비번 변경·삭제)
- Streamlit Secrets: `ADMIN_ID` / `ADMIN_PASSWORD` 자동 계정 생성
- 대진표 생성 권한: 관리자만 (사이드바 비밀번호 입력 방식 제거)

| 페이지 | 비로그인 | 회원 | 관리자 |
|--------|---------|------|--------|
| 📊 스코어보드 | ❌ 차단 | ✅ | ✅ |
| 📋 대진표생성 | ❌ 차단 | ✅ | ✅ + 생성 가능 |
| 👥 회원명부 | 🔍 구분·성명·연락처만 | ✅ 전체 | ✅ 전체 + 수정·등록 |

**회원명부 ↔ 대진표생성 완전 연동 (구글 시트 단일 진실의 원천)**
- 구글 시트 `league` 컬럼 자동 마이그레이션 (없으면 자동 추가)
- 랜덤페어 리그 배정 → 구글 시트 `league` 컬럼 즉시 반영
- 회원명부 휴면 상태 → 대진표 참가자 자동 반영 (참가 제외)
- 탈퇴 회원 → 대진표 참가자 목록 완전 제외
- 회원명부 구분·성명 사이 리그 열 추가

**대진표생성 기능 추가**
- 저장된 대진표 삭제 (2단계 확인)
- 날짜에 요일 표기: `2026-05-23(토)_001`
- 버튼명 변경: 불러오기 → `Load`, 삭제 → `Del`
- 리그 간 회원 일괄 이동 expander
- 전체선택 / 전체해제 + 이동 대상 리그 상시 표시

### 🔴 버그수정

- 회원 신규 등록 오류: `_get_cookie_manager()`에 `@st.cache_resource` 적용 시 위젯 경고 발생 → `session_state` 기반 싱글톤으로 교체
- 탈퇴 상태 자동 설정 오류: `st.date_input(value=None)`이 오늘 날짜를 반환해 항상 탈퇴 처리됨 → `text_input` + 수동 검증으로 교체

### 🛠 코드품질 (-78줄)

- 죽은 코드 제거: `get_birthday_members`, `ADMIN_PASSWORD`, `DORMANT_RANGE_RE`, 중복 CSS, `_json` 별칭 등
- `bare except: pass` → `except (ValueError, TypeError)` 구체화
- 중복 렌더링 블록 → `_build_matches_df` / `_render_match_table` 헬퍼 통합
- 점수 저장 시 `is_fully_random` 모드 정보 손실 버그 수정
- 참가자 전체선택 `session_state` 충돌 버그 수정
- 회원 목록 컬럼 인덱스 충돌로 관리 버튼 사라지는 버그 수정
- API 429 에러 해결: `@st.cache_data(ttl=120)` 적용

---

## v4.02 — 2026-05-22

### 🟢 신규

**구글 시트 회원 명부 연동**
- `gspread` + Streamlit secrets(`gcp_service_account`) 인증
- `name`(C열) + `gender`(F열) 읽기, `deleted_at` 비어있는 활성 회원만 파싱
- 상태 판별 로직 (구글 시트 기준)

| 시트 조건 | 처리 |
|-----------|------|
| `deleted_at` 값 있음 | ❌ 탈퇴 → 완전 제외 |
| `leave_date` 값 있음 | ❌ 탈퇴 → 완전 제외 |
| `dormant_period` 값 있음 | 💤 휴면 표시 |
| 위 모두 없음 | ✅ 정상 표시 |

- 리그 미지정 회원 `미배정` 버킷 임시 보관 후 앱에서 리그 배정
- `📥 구글 시트 가져오기` 버튼 추가
- 참가자 선택 팝업 이름 뒤 아이콘으로 휴면 표시
- 사이드바 요약: 휴면 포함 시 별도 카운트 표시

### 🔵 변경

- 완전 랜덤페어를 기본 선택으로 변경

---

## v4.01 — 2026-05-22

### 🔴 버그수정

**랜덤페어 복귀 시 대진표 사라짐 완전 해결**
- 원인: `if (generate_btn and pw_ok) or (do_regen and ...)` 조건이 `False`가 되면 else 브랜치가 안내문만 표시하고 `session_state`의 기존 schedule을 무시했음
- 수정: else 브랜치 진입 시 `st.session_state["rp_schedule"]`와 `last_gen_params`가 있으면 그대로 대진표 재렌더링 → 점수판↔랜덤페어 몇 번 왔다가도 유지

### 🟢 신규

**저장된 대진표 불러오기**
- 날짜 + 일련번호로 저장된 대진표를 선택해 불러오기
- 불러오면 대진표 바로 화면 표시, 재생성 버튼 활성화

**회원 사전 등록 + 선택 방식**
- 리그별 이름/성별 입력 또는 일괄 붙여넣기로 등록 (shelve 영구 저장)
- 사이드바 `참가자 선택` 버튼 → `@st.dialog` 팝업으로 변경
- 팝업 내 리그별 탭 구분, 선택 현황 실시간 카운트

**전체선택 / 해제 버그 수정**
- 원인: `value=select_all`로 checkbox 초기화 시 Streamlit이 두 번째 렌더링부터 무시
- 수정: 전체선택/해제를 별도 버튼으로 분리, `st.session_state` 직접 일괄 설정 후 `st.rerun()`

**리그 이동 기능**
- 회원 관리 expander가 리그별 탭 구조로 변경
- 이동 대상 selectbox + 이동 버튼으로 리그 간 즉시 이동

---

## v4.00 — 2026-05-22

### 🟢 신규

**페어링 방식 선택 섹션**
- 조건부 랜덤페어: 리그별 우선순위·쿼터 적용 (기존 로직)
- 완전 랜덤페어: 완전 무작위 (단, 남 vs 여 대결 불가)
- 완전 랜덤용 별도 매치 생성 함수 추가 (잡복 최소화)

**리그 수 변동 설정 기능**
- 사이드바 상단에 리그 수 설정 (1~5개)
- 리그 수에 따라 A리그, B리그, C리그… 자동 생성
- 각 리그별 독립적으로 우선순위 설정 (동성/혼복 우선, 완전랜덤)

**기타 신규**
- 재생성 버튼: 대진표 생성 후 탭 위에 배치, 비밀번호 재입력 불필요 (`session_state` 활용)
- 카카오톡 복사: 라운드별 경기 목록을 텍스트로 변환, 클립보드 복사 버튼 (JS)
- QR코드: `qrcode` 라이브러리로 현재 앱 URL QR 생성, 대진표 탭 하단 표시
- 캡션에 리그별 참가 인원수 표기

---

## v3.00

### 🟢 신규

- 점수판 (대진표 연동) 생성
- 날짜 + 일련번호로 과거 데이터 저장 기능
- 실시간 점수 확인 UI
- 랜덤 대진표 생성 시 날짜·일련번호 입력 → 점수판과 연동
- 대진표 생성 시 비밀번호 보안 추가

---

## v2.04

### 🔵 변경

- B리그 혼복 최대 2회, 동성 최소 1회 이상 쿼터 조건 추가
- B리그 매칭 우선순위: 1순위 혼복, 2순위 동성(남복·여복), 3순위 잡복

---

## v2.03

### 🔵 변경

**리그별 매칭 우선순위 분리**
- A리그: 1순위 동성(남복·여복) → 2순위 혼복 → 3순위 잡복
- B리그: 1순위 혼복 → 2순위 동성(남복·여복) → 3순위 잡복

---

## v2.02

### 🔵 변경

- 매치 종류 표기 통일: `혼복 ★중복포함` → `혼복(중복)`
- 대진표 탭 제목에 시드 표기: `경기 대진표 · 시드 #42` / `경기 대진표 · 시드 없음(랜덤)`
- 엑셀 파일명에 시드 반영: `TELA_대진표_시드42.xlsx` / `TELA_대진표_랜덤.xlsx`

---

## v2.00

### 🔴 버그수정

**`build_one_group` — anchor 강제 포함**
- 기존: 남자 4명 이상이면 무조건 남복 → 여자 1명이 매 라운드 leftover
- 수정: `pool[0]`(game_count 최소 선수)를 anchor로 확정 후 나머지 3명 구성 → 최소 경기자가 반드시 매 라운드 배정

**`make_round_matches` — leftover 교체 로직**
- 기존: leftover 방치 또는 추가 경기 강제 생성 → 5회 초과 또는 누락
- 수정: leftover 발생 시 기존 그룹의 최다 경기자와 1:1 교체 (매치 우선순위가 나빠지지 않는 경우만 허용)

**`build_event_round` — while 반복 + local_counts 추적**
- 기존: 1회 실행으로 종료 → 미달자가 이벤트 1회 추가 후 2회에서 종료
- 수정: 미달자가 0이 될 때까지 반복, 각 iteration마다 `local_counts`로 누적 추적

### 🟢 신규

- 직접 이름 입력 모드: `홍길동 남 / 김영희 여` 형식으로 실명 입력 → 대진표에 이름 표시
- 시드 고정: 동일 결과 재현 가능 (공유·디버깅용)
- 검증 리포트 탭: 미달/초과 선수 자동 감지, 혼성 경기 균등 분배 편차 경보

---

## v1.06

### 🔴 버그수정

- 불필요한 중복 게임 삭제 및 미출전자 등록(혼복) 오류 수정

### 🔵 변경 / 개선

- 우선순위 재정립: 동성 → 혼복 → 잡복 순
- 출전현황 상세 표기 (라운드별 복식 명칭 표기)
- 출전현황 표 사이즈 확대
- 출전현황 표 리그별/성별 색상 구분

---

## v1.05

### 🔵 변경 / 개선

- 혼성 최소 출전자 우선 선발: 혼성(혼복·잡복) 자리 생길 경우 혼성 경기를 가장 적게 뛴 선수를 최우선 선발
- 출전현황 표기 변경: 라운드별 출전횟수 대신 남복·혼복·여복·잡복 명칭으로 표기

---

## v1.04

### 🔵 변경 / 개선

- 잡복 최소화 알고리즘 도입
- 중복 표기 기능 추가
